# Netlify Publish Configuration
# Only publish frontend files, exclude backend
[build]
  publish = "."
  ignore = "exit 1"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
